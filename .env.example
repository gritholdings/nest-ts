# Copy this file to .env and fill in real values.

# SQLite database file (path is resolved relative to the prisma/ directory).
DATABASE_URL="file:./dev.db"

# Secret used to sign the session cookie. Use a long random value per environment.
SESSION_SECRET="replace-with-a-long-random-string"
